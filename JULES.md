## 施工事例追加時の固定ルール

今後ONE★STARへ施工事例を追加するときは、必ず以下をセットで処理してください。

1. **新しい施工事例詳細ページを作成**: 新しいディレクトリと `index.html`、画像ファイルを配置する。
2. **施工事例トップへ新しいカードを追加**: 写真・地域・メーカー・タイプなどを含む。
3. **施工事例トップの総件数を+1**: `<span class="relative inline-block font-impact...">31</span>件` 等の箇所を更新する。
4. **地域別施工事例の総件数を+1**: `works/area/xxx/index.html` の `<title>` や見出し等の総件数。
5. **該当地域の件数を+1**: `works/area/index.html` 等の表示件数。
6. **メーカー別施工事例の総件数を+1**: `works/maker/xxx/index.html`。
7. **該当メーカーの件数を+1**: `works/maker/index.html` 等の表示件数。
8. **タイプ別施工事例の総件数を+1**: `works/type/xxx/index.html`。
9. **該当タイプの件数を+1**: `works/type/index.html` 等の表示件数。
10. **該当地域の個別一覧ページへ追加**: 既存番号の最大値を確認し、次の連番を使用して一覧の**一番下**に追加する（先頭追加やNEWバッジは不可）。
11. **該当メーカーの個別一覧ページへ追加**: 既存番号の最大値を確認し、次の連番を使用して一覧の**一番下**に追加する。
12. **該当タイプの個別一覧ページへ追加**: 既存番号の最大値を確認し、次の連番を使用して一覧の**一番下**に追加する。
13. **NEWバッジの表示ルール**: NEWバッジ（`NEW!`）は**施工事例トップページ (`works/index.html`) の最新カードのみ**に付ける。各種分類ページには付けない。
14. **ドレンパンの分解洗浄に関して（表示する）**: ドレンパンを実際に取り外した施工事例の場合のみ、`<!-- ⚠️ ドレンパン分解に関する注意書きセクション -->` の共通ボックスを表示する。
15. **ドレンパンの分解洗浄に関して（表示しない）**: ドレンパンを取り外していない施工では、この共通ボックスは一切表示しない。
16. **ページネーションの更新**: 既存の最新事例の「Next」リンクを新しい事例に向けるなど、前後の施工事例へのリンク（ページネーション）を正しく更新する。

**注意事項**:
* 既存の施工事例番号を振り直さない。
* ユーザーから提供された本文テキスト（ご依頼内容、担当コメント等）は、事実関係を変えたり推測で書き直したりせず、一字一句そのまま（または指定された文脈通りに）使用する。
* 既存ページのデザイン・レイアウト・CSS・構造を不必要に変更しない。
